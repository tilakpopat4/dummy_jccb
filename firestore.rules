rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write across all collections for JCCB Gold Loan Central Portal sync
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
